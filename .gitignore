# Build artefacts
build/
DerivedData/
*.o
*.dia

# Xcode
xcuserdata/
*.xcuserstate
*.xcscmblueprint

# The Xcode project is generated from project.yml by XcodeGen — run
# `xcodegen generate` after cloning or after adding files.
SayVoice.xcodeproj/

# macOS
.DS_Store

# Whisper models are not stored in the repository: they weigh hundreds of
# megabytes and the app downloads them on first run.
*.bin

# Local Claude Code settings
.claude/settings.local.json

# Local preview config — tied to the session, not to the repository
.claude/launch.json

# Subagent-driven-development scratch (ledgers, briefs, reports)
.superpowers/
