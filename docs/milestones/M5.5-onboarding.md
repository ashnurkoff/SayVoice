# M5.5 — Onboarding + Launch at Login

**Цель:** Пошаговый мастер при первом запуске (mic + accessibility + model). Toggle автозапуска в настройках.

**Оценка:** 1 рабочий день
**Зависимости:** M5.1 завершён (settings.hasCompletedOnboarding, settings.launchAtLogin)
**Критерий готовности:** Первый запуск → мастер, второй → обычный режим. Toggle автозапуска работает.

---

## Задачи

### 1. OnboardingView.swift

```swift
// SayVoice/UI/OnboardingView.swift
import SwiftUI

struct OnboardingView: View {
    let permissionManager: PermissionManager
    let modelManager: ModelManager
    var onComplete: () -> Void

    @State private var step: OnboardingStep = .welcome

    enum OnboardingStep {
        case welcome
        case microphone
        case accessibility
        case modelDownload
        case complete
    }

    var body: some View {
        VStack(spacing: 20) {
            switch step {
            case .welcome:
                WelcomeStep(onNext: { step = .microphone })
            case .microphone:
                MicrophoneStep(
                    permissionManager: permissionManager,
                    onNext: { step = .accessibility }
                )
            case .accessibility:
                AccessibilityStep(
                    permissionManager: permissionManager,
                    onNext: {
                        step = modelManager.isModelAvailable(.small) ? .complete : .modelDownload
                    }
                )
            case .modelDownload:
                ModelDownloadStep(
                    modelManager: modelManager,
                    onNext: { step = .complete }
                )
            case .complete:
                CompleteStep(onDismiss: onComplete)
            }
        }
        .padding(24)
        .frame(width: 400, height: 300)
        .onAppear { checkInitialStep() }
    }

    private func checkInitialStep() {
        if !permissionManager.isMicrophoneGranted { step = .microphone; return }
        if !permissionManager.isAccessibilityGranted { step = .accessibility; return }
        if !modelManager.isModelAvailable(.small) { step = .modelDownload; return }
        step = .complete
    }
}
```

**Шаги мастера:**

```
┌───────────────────────────────────────┐
│  🎙  SayVoice                         │
│                                       │
│  Голосовой ввод текста.               │
│  Нужны разрешения для работы.         │
│                                       │
│              [Начать настройку]        │
└───────────────────────────────────────┘
          ↓
┌───────────────────────────────────────┐
│  🎤  Доступ к микрофону               │
│                                       │
│  Нужен для записи голоса.             │
│                                       │
│              [Разрешить]              │
└───────────────────────────────────────┘
          ↓
┌───────────────────────────────────────┐
│  ♿  Accessibility                     │
│                                       │
│  Нужен для глобального хоткея         │
│  и вставки текста.                    │
│                                       │
│  [Открыть настройки]  [Проверить]     │
└───────────────────────────────────────┘
          ↓
┌───────────────────────────────────────┐
│  ⬇  Модель Whisper Small              │
│  465 MB · хорошее качество RU/EN      │
│                                       │
│  ████████░░░░  52%                    │
│                                       │
│              [Скачать]                │
└───────────────────────────────────────┘
          ↓
┌───────────────────────────────────────┐
│  ✅  Готово!                           │
│                                       │
│  Зажмите ⌥ Right Option               │
│  чтобы начать запись.                 │
│                                       │
│              [Начать]                 │
└───────────────────────────────────────┘
```

### 2. Launch at Login (SMAppService)

**Файл:** `SayVoice/Settings/SettingsStore.swift`

```swift
import ServiceManagement

// В didSet для launchAtLogin:
var launchAtLogin: Bool {
    didSet {
        UserDefaults.standard.set(launchAtLogin, forKey: SettingsKeys.launchAtLogin)
        updateLaunchAtLogin(enabled: launchAtLogin)
    }
}

private func updateLaunchAtLogin(enabled: Bool) {
    do {
        if enabled {
            try SMAppService.mainApp.register()
        } else {
            try SMAppService.mainApp.unregister()
        }
    } catch {
        print("[SayVoice] Launch at login failed: \(error)")
    }
}
```

**Требования:**
- macOS 13.0+ (SMAppService)
- Приложение подписано (Developer ID или ad-hoc для разработки)
- Пользователь видит в System Settings → General → Login Items

### 3. Интеграция с AppCoordinator

```swift
// В start():
if !settingsStore.hasCompletedOnboarding {
    showOnboardingWindow()
} else {
    // Текущая логика: check model, start hotkey
}

private func showOnboardingWindow() {
    let view = OnboardingView(
        permissionManager: permissionManager,
        modelManager: modelManager,
        onComplete: { [weak self] in
            self?.settingsStore.hasCompletedOnboarding = true
            self?.onboardingWindow?.close()
            self?.onboardingWindow = nil
            // Запустить обычный flow
        }
    )
    // NSWindow + NSHostingView, center, makeKeyAndOrderFront
}
```

---

## Критерии готовности M5.5

### Onboarding
- [x] Первый запуск (hasCompletedOnboarding = false) → окно мастера
- [x] Второй запуск → мастер не показывается
- [x] Каждый шаг работает: welcome → mic → accessibility → model → complete
- [x] Пропуск шагов если разрешение уже дано (mic показывает checkmark)
- [x] После "Начать" → приложение готово к работе, хоткей работает

### Launch at Login
- [x] Toggle ON → `SMAppService.mainApp.register()` без ошибок
- [x] Toggle OFF → `SMAppService.mainApp.unregister()`
- [x] Приложение появляется в System Settings → Login Items
- [x] 0 errors, 0 warnings

---

## Статус: ✅ ЗАВЕРШЁН (2025-02-25)

OnboardingView: 5 шагов (Welcome → Microphone → Accessibility → Model Download → Complete).
Accessibility шаг: tccutil reset для fresh binary + polling каждую секунду.
SettingsStore: SMAppService.mainApp для Launch at Login.
AppCoordinator: onboarding → startHotkeyAndPermissions() с защитой от сброса свежего TCC.
PermissionManager: resetAccessibilityEntry() вынесен из AppCoordinator для переиспользования.
