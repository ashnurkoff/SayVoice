# M5.4 — История транскрипций

**Цель:** Сохранение истории транскрипций с отображением в popover из menu bar. Копирование по клику.

**Оценка:** 1 рабочий день
**Зависимости:** нет (параллельно с M5.1-M5.3)
**Критерий готовности:** Клик на иконку → popover с последними 10 записями, копирование по клику

---

## Задачи

### 1. TranscriptionEntry.swift

```swift
// SayVoice/History/TranscriptionEntry.swift
import Foundation

struct TranscriptionEntry: Codable, Identifiable, Sendable {
    let id: UUID
    let date: Date
    let text: String
    let durationSeconds: Double
    let language: String?   // "ru", "en", nil = авто

    init(text: String, durationSeconds: Double, language: String? = nil) {
        self.id = UUID()
        self.date = Date()
        self.text = text
        self.durationSeconds = durationSeconds
        self.language = language
    }
}
```

### 2. TranscriptionHistoryStore.swift

```swift
// SayVoice/History/TranscriptionHistoryStore.swift
import Foundation
import Observation

@MainActor @Observable
final class TranscriptionHistoryStore {
    private(set) var entries: [TranscriptionEntry] = []
    private let maxEntries = 500

    nonisolated static let storageURL: URL = {
        FileManager.default.urls(for: .applicationSupportDirectory, in: .userDomainMask).first!
            .appendingPathComponent("SayVoice/history.json")
    }()

    init() { load() }

    func append(_ entry: TranscriptionEntry) {
        entries.insert(entry, at: 0)
        if entries.count > maxEntries {
            entries = Array(entries.prefix(maxEntries))
        }
        save()
    }

    func clear() {
        entries = []
        save()
    }

    func recent(_ count: Int = 10) -> [TranscriptionEntry] {
        Array(entries.prefix(count))
    }

    private func save() {
        do {
            let dir = Self.storageURL.deletingLastPathComponent()
            try FileManager.default.createDirectory(at: dir, withIntermediateDirectories: true)
            let data = try JSONEncoder().encode(entries)
            try data.write(to: Self.storageURL, options: .atomic)
        } catch {
            print("[SayVoice] History save failed: \(error)")
        }
    }

    private func load() {
        guard FileManager.default.fileExists(atPath: Self.storageURL.path) else { return }
        do {
            let data = try Data(contentsOf: Self.storageURL)
            entries = try JSONDecoder().decode([TranscriptionEntry].self, from: data)
        } catch {
            print("[SayVoice] History load failed: \(error)")
            entries = []
        }
    }
}
```

**Заметки:**
- `nonisolated static let storageURL` — доступен из любого контекста
- `@MainActor @Observable` — как SettingsStore
- JSON хранилище: `~/Library/Application Support/SayVoice/history.json`
- max 500 записей, новые сверху

### 3. HistoryPopoverView.swift

```swift
// SayVoice/UI/HistoryPopoverView.swift
import SwiftUI

struct HistoryPopoverView: View {
    let entries: [TranscriptionEntry]
    var onClear: () -> Void
    var onSettings: () -> Void

    var body: some View {
        VStack(alignment: .leading, spacing: 0) {
            // Header
            HStack {
                Label("SayVoice", systemImage: "waveform").font(.headline)
                Spacer()
                Button(action: onSettings) {
                    Image(systemName: "gear")
                }.buttonStyle(.plain)
            }
            .padding(.horizontal, 12)
            .padding(.vertical, 10)

            Divider()

            // History list
            if entries.isEmpty {
                VStack(spacing: 4) {
                    Text("Нет записей").foregroundStyle(.secondary)
                    Text("Зажмите ⌥ Right Option для записи")
                        .font(.caption).foregroundStyle(.tertiary)
                }
                .frame(maxWidth: .infinity)
                .padding(.vertical, 20)
            } else {
                ScrollView {
                    LazyVStack(spacing: 0) {
                        ForEach(entries) { entry in
                            HistoryRowView(entry: entry)
                            Divider()
                        }
                    }
                }
                .frame(maxHeight: 300)
            }

            Divider()

            // Footer
            HStack {
                if !entries.isEmpty {
                    Button("Очистить") { onClear() }
                        .foregroundStyle(.red)
                        .buttonStyle(.plain).font(.caption)
                }
                Spacer()
                Button("Настройки...") { onSettings() }
                    .buttonStyle(.plain).font(.caption)
            }
            .padding(.horizontal, 12)
            .padding(.vertical, 8)
        }
        .frame(width: 320)
    }
}

struct HistoryRowView: View {
    let entry: TranscriptionEntry
    @State private var copied = false

    var body: some View {
        Button(action: copyToClipboard) {
            HStack(alignment: .top, spacing: 8) {
                VStack(alignment: .leading, spacing: 2) {
                    Text(entry.text)
                        .font(.system(size: 12))
                        .lineLimit(2)
                        .multilineTextAlignment(.leading)
                    HStack(spacing: 4) {
                        Text(entry.date, style: .relative)
                        Text("·")
                        Text("\(entry.durationSeconds, specifier: "%.1f")с")
                    }
                    .font(.caption).foregroundStyle(.secondary)
                }
                Spacer()
                Image(systemName: copied ? "checkmark" : "doc.on.doc")
                    .font(.caption).foregroundStyle(.secondary)
            }
            .padding(.horizontal, 12)
            .padding(.vertical, 6)
            .contentShape(Rectangle())
        }
        .buttonStyle(.plain)
    }

    private func copyToClipboard() {
        NSPasteboard.general.clearContents()
        NSPasteboard.general.setString(entry.text, forType: .string)
        withAnimation { copied = true }
        Task { @MainActor in
            try? await Task.sleep(for: .seconds(1.5))
            withAnimation { copied = false }
        }
    }
}
```

### 4. Popover из menu bar

**MenuBarController.swift — изменения:**

Текущая реализация: `statusItem.menu = menu` (правый клик = меню).
Для popover нужно:
- Левый клик → popover с историей
- Правый клик → меню (quit, settings, download)

**Вариант реализации:**
```swift
// Убрать statusItem.menu = menu
// Вместо этого: button.action = #selector(handleClick)
// В handleClick: показать NSPopover с HistoryPopoverView
// Для правого клика: button.sendAction(on: [.leftMouseUp, .rightMouseUp])
// В handleClick проверить currentEvent: если rightMouseUp → показать menu
```

**NSPopover setup:**
```swift
private lazy var popover: NSPopover = {
    let p = NSPopover()
    p.behavior = .transient  // Закрывается при клике вне
    p.contentSize = NSSize(width: 320, height: 400)
    return p
}()
```

### 5. Сохранение записей из AppCoordinator

**AppCoordinator.swift:**
```swift
private let historyStore = TranscriptionHistoryStore()

// В handleKeyUp(), после успешной транскрипции:
let entry = TranscriptionEntry(
    text: text,
    durationSeconds: Double(samples.count) / 16000.0
)
historyStore.append(entry)
```

---

## Критерии готовности M5.4

- [x] Клик на иконку в menu bar → popover с историей
- [x] Правый клик → контекстное меню (Quit, Settings)
- [x] После транскрипции → новая запись в истории
- [x] Клик на запись → текст скопирован, иконка → checkmark на 1.5 сек
- [x] "Очистить" → список пустеет
- [x] Перезапуск → история загружена из JSON
- [x] Пустая история → placeholder "Нет записей"
- [x] Max 500 записей, новые сверху
- [x] 0 errors, 0 warnings

---

## Статус: ✅ ЗАВЕРШЁН (2025-02-25)

Создано 3 новых файла: TranscriptionEntry.swift, TranscriptionHistoryStore.swift, HistoryPopoverView.swift.
Переработан MenuBarController: левый клик → NSPopover с историей, правый клик → контекстное меню.
AppCoordinator: historyStore + сохранение записей после транскрипции + onPopoverWillShow для свежих данных.
